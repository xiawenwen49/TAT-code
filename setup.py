from setuptools import setup
from setuptools import find_packages

setup(name='tat',
      version='0.1',
      description='',
      license='MIT',
      package_dir={'': './'},
      packages=['TAT'],
      zip_safe=False)

